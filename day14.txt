상속 : 기존의 클래스의 기능을 가져다 사용하는 것
        상속을 하면 오버라이딩이 가능하다(매개변수가 같은 경우 부모클래스와 자녀클래스 둘 다 나옴, 부모부터 순서대로 출력)
        extends를 통해 상속받는다
        자녀 클래스에서 super을 사용하면 부모클래스에서 ()에 넣은 매개변수와 동일한 메서드 실행
        부모클래스와 자식클래스에 같은 메서드가 있으면 자기자신에게 있는 메서드 우선 출력함

this.a05(); 자신에 있는 메서드
super.a05(); 부모에 있는 메서드
